[tool.pytest.ini_options]
# `mini_sglang_mlx` is a namespace package (no __init__.py), so the repo root has to
# be importable as a path rather than discovered as an installed package.
pythonpath = ["."]
testpaths = ["mini_sglang_mlx/tests"]
